import sys
print("Step 1: start", flush=True)

import numpy as np
print("Imported numpy", flush=True)

import scipy
print("Imported scipy", flush=True)

import sklearn
print("Imported sklearn", flush=True)

import huggingface_hub
print("Imported huggingface_hub", flush=True)

import transformers
print("Imported transformers", flush=True)

